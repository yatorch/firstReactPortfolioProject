import React from 'react';
import styles from './styles/Track.module.css';

// Stateless component, made to contain each track
// Tracks passed down from: Sbar -> PageContainer -> RContainer -> SearchResults -> Track OR TrackList -> Track on last leg

export default function Track(props) {
    return (
        <div className={styles.trackMainContainer}>
            
        </div>
    )
}